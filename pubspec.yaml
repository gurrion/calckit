name: calckit
description: All-in-One Calculator Kit. Basic, loans, BMI, tips, discounts, unit converter.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_mobile_ads: ^5.3.0
  in_app_purchase: ^3.2.0
  shared_preferences: ^2.3.0
  provider: ^6.1.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
